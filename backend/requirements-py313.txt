# Requirements for Python 3.13
# Note: Some packages may need to compile from source
# Consider using Python 3.11 or 3.12 for easier installation

# Core Dependencies
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ML Dependencies - Use latest versions for Python 3.13 support
# May require build tools: brew install gcc cmake
scikit-learn>=1.5.0  # Newer version with better 3.13 support
numpy>=1.26.0
pandas>=2.2.0

# Alternative: Install pre-release versions
# pip install --pre scikit-learn

# Simpler ML libraries (install these first if sklearn fails)
# Then use simple linear regression or decision trees
lightgbm>=4.1.0  # Usually has good support
# xgboost>=2.0.2  # Comment out if fails

# Stock Data Fetching
yfinance>=0.2.32
requests>=2.31.0

# Database (Optional)
sqlalchemy>=2.0.23
aiosqlite>=0.19.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6
aiofiles>=23.2.1

# CORS and Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Testing
pytest>=7.4.3
httpx>=0.25.2

# Logging
loguru>=0.7.2

# Job scheduling
joblib>=1.3.2
